export const environment = {
  production: true,
  appName: 'El Dugout Ve',
  // In production on Cloud Run, if front and backend share domain or load balancer,
  // relative '/api' is used; otherwise set to the Cloud Run Backend service URL.
  apiUrl: '/api',
};
